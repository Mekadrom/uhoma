group = 'com.higgs'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
}

bootJar {
    dependsOn processResources
}

bootRun {
    jvmArgs = ["-Dlog4j.debug"]
}

processResources {
    filesMatching('**/*.yml') {
        expand(project.properties)
        expand(ext.properties)
    }
}

springBoot {
    buildInfo()
}
